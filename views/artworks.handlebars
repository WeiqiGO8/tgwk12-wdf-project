<section class="text-container-light projects-container">
	<h1>Artworks</h1>

	<article>
		{{#each artworks}}
			<figure>
				<a href="/artworks/{{aid}}">
					<img src="{{aurl}}" alt="{{alturl}}" />
				</a>
			</figure>
		{{/each}}
	</article>

</section>