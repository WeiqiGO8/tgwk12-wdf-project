<section class="text-container-light">
	<h1>work for</h1>
	<section>
		<ul>
			{{#each workfor}}
				<li>
					{{ftype}}
					{{fname}}
					{{fdesc}}
					{{ffor}}
				</li>
			{{/each}}
		</ul>
	</section>
</section>