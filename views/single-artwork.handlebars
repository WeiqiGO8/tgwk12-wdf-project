<section class="text-container-light single-artwork-container">

	<article>
	<h1>{{artwork.aname}}</h1>
		<img src="/{{artwork.aurl}}" alt="{{artwork.alturl}}" />
		<ul>
			<li>
				{{artwork.atype}}
			</li>
			<li>
				{{artwork.ayear}}
			</li>
			<li>
				{{artwork.afor}}
			</li>
			<li>
				{{artwork.adesc}}
			</li>
		</ul>
	</article>
</section>