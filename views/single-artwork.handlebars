<section class="text-container-light detail-container">

	<h1>{{artwork.aname}}</h1>
	<article>
		<img src="/{{artwork.aurl}}" alt="{{artwork.alturl}}" />
		<ul>
			{{! artwork table info }}
			<li>
				{{artwork.adesc}}
			</li>
			<li>
				{{artwork.atype}}
			</li>
			<li>
				{{artwork.ayear}}
			</li>
			{{! workfor table info }}
			<li>
				{{artwork.fname}}
			</li>
			<li>
				{{artwork.fdesc}}
			</li>
		</ul>
	</article>
</section>