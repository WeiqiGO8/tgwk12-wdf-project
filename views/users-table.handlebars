<section class="text-container-light users-table">

	<h1>Users table</h1>

	<article>
		<table>
			{{! headers of the table }}
			<tr>
				<th>Username</th>
			</tr>

			{{#each usersTable}}
				<tr>
					<td>{{username}}</td>
				</tr>
			{{/each}}
		</table>
	</article>

</section>